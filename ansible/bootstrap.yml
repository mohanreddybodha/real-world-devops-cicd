
- hosts: all
  become: yes
  gather_facts: no

  tasks:
    - name: Install Python + SELinux libs
      raw: |
        dnf install -y \
          python3 \
          python3-libselinux \
          libselinux \
          sudo
        ln -sf /usr/bin/python3 /usr/bin/python